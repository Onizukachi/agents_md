# Новый статус заказа «Охлаждение» для TBankCredit и Vanta

## 1. Бизнес-контекст для задачи

Для рассрочек TBankCredit и Vanta банк может установить обязательный период охлаждения после одобрения и подписания кредитного договора. В этот период денежные средства уже учитываются как замороженные, но рассрочка ещё может быть отменена клиентом.

Текущее поведение не отражает этот этап в статусе заказа:

- TBankCredit при callback `signed` замораживает платеж и переводит заказ в `paid`, даже если `commitCooldown.hasHappened == false`;
- Vanta при `blank.cooled` замораживает платеж и сохраняет срок охлаждения, но оставляет заказ в `approved`;
- отмена Vanta во время охлаждения напрямую обнуляет `frozen_amount`, а TBankCredit освобождает замороженную сумму через `mark_released!`; единое ожидаемое поведение заказа отсутствует.

Нужно добавить строковый AASM-статус заказа `cooling` с пользовательским названием «Охлаждение». На первом этапе новый сценарий применяется только к TBankCredit и Vanta.

Ожидаемый бизнес-процесс:

1. Само одобрение рассрочки не меняет статус заказа.
2. Когда подтверждено фактическое начало периода охлаждения:
   - платеж переводится в замороженное состояние;
   - срок охлаждения сохраняется в metadata платежа;
   - заказ переводится из `approved` в `cooling`;
   - автобронирование не запускается.
3. Когда охлаждение завершилось и рассрочка не отменена:
   - выполняется предусмотренное процессором списание/capture;
   - после подтвержденного успешного завершения финансовой операции заказ переводится из `cooling` в `paid`;
   - стандартные сопутствующие действия статуса `paid`, включая постановку автобронирования, сохраняются.
4. Когда клиент отменяет рассрочку во время охлаждения:
   - если у платежа есть замороженная сумма, она освобождается через `payment.mark_released!`;
   - cooling metadata сбрасывается;
   - кредит/заявка переводится в соответствующий отмененный статус;
   - заказ переводится в `canceled` через `order.cancel!`, чтобы выполнились все стандартные callbacks отмены.
5. Изменение цены package во время охлаждения не создает отдельную ветку:
   - цена не сравнивается с ценой на момент входа в `cooling`;
   - при успешном завершении охлаждения финансовая операция завершается обычным образом;
   - заказ всегда переводится из `cooling` в `paid`;
   - после перехода в `paid` запускается существующий процесс автобронирования с его текущими проверками актуальной цены;
   - эта задача не изменяет правила проверки цены, допустимого расхождения или обработки отказа автобронирования.

## 2. Ключевые решения и обоснования

### Статус и переходы заказа

- Техническое имя нового статуса: `cooling`; отображаемое имя: «Охлаждение».
- `cooling` считается неоплаченным состоянием и включается в `Order.not_paid`.
- Основной переход начала охлаждения: `approved -> cooling`.
- Успешное завершение: `cooling -> paid` через существующий или расширенный AASM-event оплаты, с сохранением стандартных callbacks `paid`.
- Отмена: `cooling -> canceled` через существующий `cancel!`, с добавлением `cooling` в разрешенные исходные состояния.
- Из `cooling` не запускается автобронирование.
- Миграция БД не требуется: `orders.state` уже является строковой колонкой, а состояния задаются AASM на уровне приложения.

### Точки входа по процессорам

- TBankCredit начинает охлаждение только на `signed` с присутствующим `commitCooldown`, валидным `until` и `hasHappened == false`.
- Callback TBankCredit `approved` сам по себе статус заказа не меняет.
- TBankCredit без `commitCooldown` сохраняет текущий сценарий и не переводит заказ в `cooling`.
- Завершение охлаждения TBankCredit определяется callback `signed` с `commitCooldown.hasHappened == true`.
- Vanta начинает охлаждение на событии `blank.cooled`.
- Vanta завершает финансовую часть и охлаждение на событии `blank.payed`.

### Финансовые операции

- Начало охлаждения должно оставить платеж замороженным, но не captured.
- Переход заказа в `paid` разрешен только после успешного capture/фиксации оплаченного платежа.
- После вызова TBankCredit `payment.capture!` необходимо перезагрузить платеж и проверить `payment.captured?`, поскольку commit в банк может завершиться ошибкой без перевода платежа в captured.
- При отмене замороженная сумма освобождается только через `mark_released!`; прямое обнуление `frozen_amount` не используется.
- `mark_released!` вызывается только при положительном `frozen_amount`, чтобы обработка повторных callback оставалась идемпотентной.
- Для Vanta события `blank.canceled` и `blank.auth_canceled` отменяют заказ только пока он находится в `cooling` и платеж ещё не captured.
- Текущее поведение Vanta для отмены уже captured-платежа (автоматический или ручной refund в зависимости от детализации) сохраняется отдельно.
- Поведение Vanta для `blank.rejected` и `blank.error` не расширяется до отмены заказа без отдельного бизнес-требования.

### Изменение цены во время охлаждения

- Бизнес подтвердил, что изменение цены не должно влиять на завершение cooling workflow.
- Не сохранять snapshot цены специально для cooling.
- Не искать `OrderPriceChange` и не сравнивать текущую цену с ценой при входе в `cooling`.
- Не блокировать capture и переход `cooling -> paid` из-за роста или снижения цены.
- После успешного capture/фиксации оплаты всегда вызывать стандартный переход заказа в `paid`.
- Проверка актуальной цены остается внутри существующего автобронирования, которое запускается стандартными callbacks состояния `paid`.
- Не менять в рамках этой задачи допуски автобронирования, правила сравнения цены и поведение заказа при ошибке актуализации или расхождении цены.

### Совместимость и представление статуса

- Добавить `cooling` во все обязательные карты статусов, переводы, state logging и представления, где исчерпывающе перечислены состояния заказа.
- Проверить сериализацию заказа и определить `scope` нового статуса как `not_paid`.
- Проверить manager UI, клиентские страницы/API, уведомления, задачи и выборки, которые используют `Order.not_paid`, `Order.paid_states`, `active_states` или явные списки состояний.
- Не добавлять `cooling` в `paid_states`: иначе до завершения охлаждения могут запуститься процессы, рассчитанные на оплаченный заказ.

## 3. Структурированный список задач

- [ ] Шаг 1: Зафиксировать текущее поведение model- и callback-spec для TBankCredit и Vanta как регрессионную базу перед изменениями.
- [ ] Шаг 2: Найти все исчерпывающие списки и отображения `Order` states (`Order.not_paid`, сериализаторы, I18n, state logging, manager/client UI, фильтры, уведомления и задачи) и составить точный список затрагиваемых файлов.
- [ ] Шаг 3: Добавить AASM-state `cooling` в `Order`, включить его в `Order.not_paid` и добавить разрешенные переходы `approved -> cooling`, `cooling -> paid` и `cooling -> canceled`.
- [ ] Шаг 4: Добавить доменно названный AASM-event начала охлаждения с нужными callbacks обновления задач, если существующий механизм задач требует реакции на новый статус.
- [ ] Шаг 5: Добавить сообщение `cooling` в `OrderStateLogging::STATE_MESSAGES` и обеспечить логирование каждого автоматического перехода `approved -> cooling`, `cooling -> paid` и `cooling -> canceled`.
- [ ] Шаг 6: Добавить русские переводы названия и отображения статуса «Охлаждение» во все используемые карты статусов и клиентские/менеджерские представления.
- [ ] Шаг 7: Добавить model specs для допустимых и недопустимых AASM-переходов нового статуса, классификации `Order.not_paid` и отсутствия запуска автобронирования при входе в `cooling`.
- [ ] Шаг 8: Выделить или расширить общий доменный workflow начала охлаждения, который атомарно сохраняет cooling metadata, замороженное состояние платежа и переводит заказ из `approved` в `cooling`, не запуская callbacks оплаты.
- [ ] Шаг 9: Выделить или расширить общий workflow отмены охлаждения, который вызывает `mark_released!` при положительном `frozen_amount`, сбрасывает cooling metadata и вызывает `order.cancel!`; обеспечить безопасность повторного callback.
- [ ] Шаг 10: Изменить TBankCredit `signed` с `commitCooldown.hasHappened == false`: подписать кредит, заморозить платеж, сохранить cooling metadata и перевести заказ в `cooling`, не вызывая `mark_paid!` и capture.
- [ ] Шаг 11: Сохранить без изменений сценарий TBankCredit `signed` без `commitCooldown`, включая текущий переход заказа в `paid`.
- [ ] Шаг 12: Изменить TBankCredit завершение охлаждения (`hasHappened == true`): сбросить cooling metadata, выполнить `capture!`, проверить `captured?` после reload и при успехе всегда перевести заказ из `cooling` в `paid`, не проверяя изменение цены.
- [ ] Шаг 13: Изменить TBankCredit `canceled` во время `cooling`: освободить положительный `frozen_amount` через `mark_released!`, отменить кредит и вызвать `order.cancel!` со стандартными сопутствующими действиями.
- [ ] Шаг 14: Дополнить TBankCredit callback specs сценариями начала охлаждения, успешного завершения, ошибки capture, клиентской отмены и повторных callback; отдельно подтвердить, что изменение цены не блокирует `cooling -> paid`.
- [ ] Шаг 15: Изменить Vanta `blank.cooled`: после проверки срока заморозить платеж, сохранить cooling metadata и перевести заказ из `approved` в `cooling` в одной транзакции.
- [ ] Шаг 16: Изменить Vanta `blank.payed`: завершить охлаждение, создать требуемый чек, зафиксировать capture и всегда перевести заказ из `cooling` в `paid`, не проверяя изменение цены.
- [ ] Шаг 17: Изменить Vanta `blank.canceled` и не-captured `blank.auth_canceled` во время `cooling`: заменить прямое обнуление заморозки на `mark_released!`, сбросить cooling metadata и вызвать `order.cancel!`.
- [ ] Шаг 18: Сохранить существующее поведение Vanta для `blank.rejected`, `blank.error` и `blank.auth_canceled` после capture, добавив регрессионные тесты на отсутствие неожиданных переходов заказа.
- [ ] Шаг 19: Дополнить Vanta callback specs сценариями `approved -> cooling`, `cooling -> paid`, отмены с `mark_released!` и `order.cancel!`, повторных событий и изменения цены без блокировки перехода в `paid`.
- [ ] Шаг 20: Проверить фискальные последствия: TBankCredit сейчас создает advanced receipt через `mark_authorized!` при заморозке, а Vanta создает его при завершении охлаждения; сохранить текущую процессорную семантику, если тесты и действующие требования не указывают на необходимость унификации.
- [ ] Шаг 21: Проверить гонки callback: отмена и завершение охлаждения, повторное `signed`/`blank.payed`, завершение после уже отмененного заказа и отмена после capture; операции должны быть транзакционными и идемпотентными.
- [ ] Шаг 22: Запустить focused specs для `Order`, `TBankCreditCallbackProcessor`, `VantaCallbackProcessor` и затронутых cooling/payment workflows через обязательный LevelTravel test workflow.
- [ ] Шаг 23: Выполнить read-only финальную проверку diff на регрессии AASM, автобронирования, refunds, чеков, уведомлений, задач и state-based выборок.
- [ ] Шаг 24: Обновить этот артефакт фактически выполненными шагами, результатами тестов, оставшимися рисками и точкой продолжения.
- [ ] Шаг 25: Синхронизировать изменения `AGENTS.md`/`.agents/` в `../agents_md` через обязательный `leveltravel-agents-sync` workflow.

## 4. Заметки для восстановления сессии

### Текущее состояние проекта и уже выполненная работа

- Реализация нового статуса ещё не начиналась; на момент создания этого артефакта изменяется только документация задачи.
- Изучены текущие обработчики:
  - `app/apis/t_bank_credit_callback_processor.rb`;
  - `app/apis/vanta_callback_processor.rb`;
  - `app/models/payment.rb`;
  - `app/models/concerns/payment/coolable.rb`;
  - AASM заказа в `app/models/order.rb`.
- Текущий TBankCredit на `signed` вызывает в порядке: авторизацию/заморозку, обработку cooling metadata, `order.mark_paid!`, затем условный capture. Поэтому при активном охлаждении заказ преждевременно становится `paid`.
- Текущий TBankCredit `capture_t_bank_credit` синхронно вызывает commit банка и при успехе выполняет `mark_captured!`; ошибка может остаться только в логе, поэтому после `capture!` обязательна проверка `payment.reload.captured?`.
- Текущий Vanta на `blank.cooled` сохраняет cooling metadata и напрямую выставляет `frozen_amount`/`frozen_at`, не меняя state заказа.
- Текущий Vanta на `blank.payed` завершает cooling, создает advanced receipt, вызывает `mark_captured!` и затем `order.mark_paid!`.
- Текущий Vanta при `blank.canceled`/не-captured `blank.auth_canceled` сбрасывает cooling и напрямую обновляет `frozen_amount: 0, frozen_at: nil`; это нужно заменить на `mark_released!`.
- `Payment#paid?` считает платеж оплаченным при `frozen_amount > 0`, а `Order#paid_amount` учитывает замороженную сумму. Новый state решает проблему бизнес-статуса и автобронирования, но не меняет эту общую денежную семантику.
- `Order#mark_paid` сейчас разрешен только из `approved`; его нужно расширить для `cooling`.
- `Order#cancel` сейчас не принимает `cooling`; новый state нужно добавить в исходные состояния.
- `Order#update_tasks` запускает `booking!` только в ветке `state == :paid`, поэтому `cooling` не должен попадать в `paid_states` и не должен вызывать `mark_paid!` до завершения охлаждения.
- Бизнес подтвердил, что изменение цены package во время cooling не анализируется и не влияет на capture или переход заказа в `paid`; отдельная интеграция с `OrderPriceChange` не нужна.
- Миграция БД для нового AASM-state не ожидается.

### Критически важные принятые решения

- Новый state называется `cooling`, отображается как «Охлаждение» и относится к неоплаченным.
- Вход в `cooling` происходит только при подтвержденном наличии активного периода охлаждения, а не при простом одобрении рассрочки.
- TBankCredit без охлаждения сохраняет существующее поведение.
- Любая клиентская отмена во время cooling должна освобождать замороженные средства через `mark_released!` и отменять заказ именно через `order.cancel!`.
- Переход в `paid` после cooling выполняется только после успешной финансовой фиксации платежа.
- Рост или снижение цены package не создает отдельного сценария: после успешной финансовой фиксации заказ всегда переводится в `paid`.
- Cooling workflow не сохраняет snapshot цены и не обращается к `OrderPriceChange`.
- После `cooling -> paid` используется существующая автобронь и её текущие правила актуализации и проверки цены; менять их в этой задаче не нужно.

### Точка продолжения при восстановлении сессии

1. Открыть этот файл и проверить отмеченные пункты.
2. Начать с шагов 1–7: state-аудит, AASM `cooling`, классификация, логи, переводы и model specs.
3. До изменения callback processors определить минимальный общий workflow для start/cancel/complete cooling, чтобы TBankCredit и Vanta не получили расходящуюся реализацию.
4. Затем реализовать TBankCredit и Vanta отдельными небольшими изменениями с focused specs после каждого процессора.
5. Не расширять задачу на Paylate или другие платежные методы.
6. Не добавлять проверки изменения цены: успешное завершение cooling всегда должно завершать платеж и переводить заказ в `paid`.
