# Новый статус заказа «Охлаждение» для TBankCredit и Vanta

Tracker: LT-52093

## 1. Бизнес-контекст для задачи

Для рассрочек TBankCredit и Vanta банк может установить обязательный период охлаждения после одобрения и подписания кредитного договора. В этот период денежные средства уже учитываются как замороженные, но рассрочка ещё может быть отменена клиентом.

Текущее поведение не отражает этот этап в статусе заказа:

- TBankCredit при callback `signed` замораживает платеж и переводит заказ в `paid`, даже если `commitCooldown.hasHappened == false`;
- Vanta при `blank.cooled` замораживает платеж и сохраняет срок охлаждения, но оставляет заказ в `approved`;
- отмена Vanta во время охлаждения напрямую обнуляет `frozen_amount`, а TBankCredit освобождает замороженную сумму через `mark_released!`; единое ожидаемое поведение заказа отсутствует.

Нужно добавить строковый AASM-статус заказа `cooling` с пользовательским названием «Охлаждение». На первом этапе новый сценарий применяется только к TBankCredit и Vanta.

Ожидаемый бизнес-процесс:

1. Само одобрение рассрочки не меняет статус заказа.
2. Когда подтверждено фактическое начало периода охлаждения:
   - платеж переводится в замороженное состояние;
   - срок охлаждения сохраняется в metadata платежа;
   - заказ переводится из `approved` в `cooling`;
   - автобронирование не запускается.
3. Когда охлаждение завершилось и рассрочка не отменена:
   - выполняется предусмотренное процессором списание/capture;
   - после подтвержденного успешного завершения финансовой операции заказ переводится из `cooling` в `paid`;
   - стандартные сопутствующие действия статуса `paid`, включая постановку автобронирования, сохраняются.
4. Когда приходит отмена кредитной заявки:
   - если у платежа есть замороженная сумма, она освобождается через `payment.mark_released!`;
   - cooling metadata сбрасывается только у callback-платежа;
   - кредит/заявка переводится в соответствующий отмененный статус;
   - TBankCredit отменяет заказ для любого состояния кредита, кроме `committed`; `signed`, но еще не списанный кредит, также отменяет заказ;
   - Vanta `blank.canceled` переводит заказ в `canceled` через стандартный `order.cancel!`.
5. Изменение цены package во время охлаждения не создает отдельную ветку:
   - цена не сравнивается с ценой на момент входа в `cooling`;
   - при успешном завершении охлаждения финансовая операция завершается обычным образом;
   - заказ всегда переводится из `cooling` в `paid`;
   - после перехода в `paid` запускается существующий процесс автобронирования с его текущими проверками актуальной цены;
   - эта задача не изменяет правила проверки цены, допустимого расхождения или обработки отказа автобронирования.

## 2. Ключевые решения и обоснования

### Статус и переходы заказа

- Техническое имя нового статуса: `cooling`; отображаемое имя: «Охлаждение».
- `cooling` считается неоплаченным состоянием и включается в `Order.not_paid`.
- Основной переход начала охлаждения: `approved -> cooling`.
- Успешное завершение: `cooling -> paid` через существующий или расширенный AASM-event оплаты, с сохранением стандартных callbacks `paid`.
- Vanta-отмена во время cooling допускает `cooling -> canceled` через стандартный `order.cancel!`; общий event заказа не управляет разморозкой кредитных платежей.
- Из `cooling` не запускается автобронирование.
- Миграция БД не требуется: `orders.state` уже является строковой колонкой, а состояния задаются AASM на уровне приложения.

### Точки входа по процессорам

- TBankCredit начинает охлаждение только на `signed` с присутствующим `commitCooldown`, валидным `until` и `hasHappened == false`.
- Callback TBankCredit `approved` сам по себе статус заказа не меняет.
- TBankCredit без `commitCooldown` не переводит заказ в `cooling`: после подписания он авторизует и сразу списывает платеж.
- Завершение охлаждения TBankCredit определяется callback `signed` с `commitCooldown.hasHappened == true`.
- Vanta начинает охлаждение на событии `blank.cooled`.
- Vanta завершает финансовую часть и охлаждение на событии `blank.payed`.
- Оба процессора начинают охлаждение одинаково: напрямую сохраняют `frozen_amount`/`frozen_at` без вызова `payment.mark_authorized!` и без фискализации.
- Старт cooling атомарен относительно состояния заказа: `approved` переводится в `cooling`, повторный callback для уже `cooling` считается идемпотентным, а любое другое состояние прерывает транзакцию без сохранения заморозки и metadata.
- Конкурирующие callback сериализуются в едином порядке блокировок: TBankCredit `payment -> credit -> order`, Vanta `payment -> order`; после блокировок повторно проверяются terminal states.
- При отмене принадлежность cooling определяется по metadata конкретного callback-платежа; другой платеж того же заказа не размораживается.
- Если TBankCredit передал pending `commitCooldown` с отсутствующим или невалидным `until`, callback возвращает контролируемый `400 invalid_commit_cooldown` до локального подписания кредита и не попадает в обычный сценарий без cooling.
- TBankCredit `canceled` проверяет состояние до `credit.cancel!`: заказ отменяется для любого состояния, кроме `committed`, которое означает уже выполненный capture.
- `Payment::Coolable#update_cooling` строго валидирует результат парсинга перед сохранением ISO8601.
- Advanced receipt охлаждаемого платежа создается только при подтвержденном завершении охлаждения; обычный сценарий TBankCredit без cooling вызывает `mark_authorized!`, затем `capture!`.
- Проверка существующего advanced receipt и его создание сериализованы блокировкой строки платежа, чтобы параллельные финальные callback не создали дубли.

### Финансовые операции

- Начало охлаждения должно оставить платеж замороженным, но не captured.
- Переход заказа в `paid` разрешен только после успешного capture/фиксации оплаченного платежа.
- После вызова TBankCredit `payment.capture!` необходимо перезагрузить платеж и проверить `payment.captured?`, поскольку commit в банк может завершиться ошибкой без перевода платежа в captured.
- Состояние cooling хранится только в metadata платежа; legacy-флаг `PaymentCredit#data[:waiting_cool_down]` больше не записывается и не читается.
- Общий `Payment#validate_capturing` не блокирует cooling: системный callback завершения охлаждения должен иметь возможность вызвать обычный `capture!`.
- Ручной capture в менеджерке скрывается по сериализованному признаку cooling и отклоняется серверным endpoint по `payment.cooling_metadata?`.
- Фоновый `PaymentCapturingService` исключает платежи по наличию cooling metadata, включая случаи, когда срок уже истек, но финальный callback еще не обработан.
- При отмене замороженная сумма освобождается только через `mark_released!`; прямое обнуление `frozen_amount` не используется.
- `mark_released!` вызывается только при положительном `frozen_amount`, чтобы обработка повторных callback оставалась идемпотентной.
- При отмене нового cooling refund receipt не создается, потому что на старте не было purchase receipt. Обычная отмена сохраняет прежнюю фискализацию, а legacy cooling с уже существующим advanced purchase receipt получает соответствующий refund receipt.
- TBankCredit и Vanta вызывают `payment.mark_released!` и сбрасывают metadata только для callback-платежа; массового освобождения платежей в `Order#cancel` нет.
- Повторный Vanta `blank.cooled` определяется по cooling metadata конкретного платежа и сразу возвращает success без запроса `payment_info`; состояние другого платежа того же cooling-заказа не блокирует обработку.
- Для Vanta событие `blank.canceled` освобождает callback-платёж и отменяет заказ.
- Vanta обрабатывает `blank.canceled` отдельно от `FAILURE_EVENTS`: frozen-платёж всегда освобождается без refund-чека, cooling metadata сбрасывается при наличии и заказ отменяется. `blank.rejected` и `blank.error` только логируются.
- `blank.auth_canceled` считается post-payment событием: refund обрабатывается только для captured-платежа, а для non-captured callback подтверждается без изменений.
- Текущее поведение Vanta для отмены уже captured-платежа (автоматический или ручной refund в зависимости от детализации) сохраняется отдельно.
- Поведение Vanta для `blank.rejected` и `blank.error` не расширяется до отмены заказа без отдельного бизнес-требования.

### Изменение цены во время охлаждения

- Бизнес подтвердил, что изменение цены не должно влиять на завершение cooling workflow.
- Не сохранять snapshot цены специально для cooling.
- Не искать `OrderPriceChange` и не сравнивать текущую цену с ценой при входе в `cooling`.
- Не блокировать capture и переход `cooling -> paid` из-за роста или снижения цены.
- После успешного capture/фиксации оплаты всегда вызывать стандартный переход заказа в `paid`.
- Проверка актуальной цены остается внутри существующего автобронирования, которое запускается стандартными callbacks состояния `paid`.
- Не менять в рамках этой задачи допуски автобронирования, правила сравнения цены и поведение заказа при ошибке актуализации или расхождении цены.

### Совместимость и представление статуса

- Добавить `cooling` во все обязательные карты статусов, переводы, state logging и представления, где исчерпывающе перечислены состояния заказа.
- Проверить сериализацию заказа и определить `scope` нового статуса как `not_paid`.
- Проверить manager UI, клиентские страницы/API, уведомления, задачи и выборки, которые используют `Order.not_paid`, `Order.paid_states`, `active_states` или явные списки состояний.
- Не добавлять `cooling` в `paid_states`: иначе до завершения охлаждения могут запуститься процессы, рассчитанные на оплаченный заказ.

## 3. Структурированный список задач

- [x] Шаг 1: Зафиксировать текущее поведение model- и callback-spec для TBankCredit и Vanta как регрессионную базу перед изменениями.
- [x] Шаг 2: Найти все исчерпывающие списки и отображения `Order` states (`Order.not_paid`, сериализаторы, I18n, state logging, manager/client UI, фильтры, уведомления и задачи) и составить точный список затрагиваемых файлов.
- [x] Шаг 3: Добавить AASM-state `cooling` в `Order`, включить его в `Order.not_paid` и добавить разрешенные переходы `approved -> cooling`, `cooling -> paid` и `cooling -> canceled`.
- [x] Шаг 4: Добавить доменно названный AASM-event начала охлаждения с нужными callbacks обновления задач, если существующий механизм задач требует реакции на новый статус.
- [x] Шаг 5: Добавить сообщение `cooling` в `OrderStateLogging::STATE_MESSAGES` и обеспечить логирование автоматических переходов `approved -> cooling`, `cooling -> paid` и `cooling -> canceled`.
- [x] Шаг 6: Добавить русские переводы названия и отображения статуса «Охлаждение» во все используемые карты статусов и клиентские/менеджерские представления.
- [x] Шаг 7: Добавить model specs для допустимых и недопустимых AASM-переходов нового статуса, классификации `Order.not_paid` и отсутствия запуска автобронирования при входе в `cooling`.
- [x] Шаг 8: Выделить или расширить общий доменный workflow начала охлаждения, который атомарно сохраняет cooling metadata, замороженное состояние платежа и переводит заказ из `approved` в `cooling`, не запуская callbacks оплаты.
- [x] Шаг 9: При отмене cooling освобождать и сбрасывать только callback-платёж, после чего отменять заказ; обеспечить безопасность повторного callback.
- [x] Шаг 10: Изменить TBankCredit `signed` с `commitCooldown.hasHappened == false`: подписать кредит, заморозить платеж, сохранить cooling metadata и перевести заказ в `cooling`, не вызывая `mark_paid!` и capture.
- [x] Шаг 11: Для TBankCredit `signed` без `commitCooldown` авторизовать и сразу списать платеж; переводить заказ в `paid` только после подтвержденного capture.
- [x] Шаг 12: Изменить TBankCredit завершение охлаждения (`hasHappened == true`): сбросить cooling metadata, выполнить `capture!`, проверить `captured?` после reload и при успехе всегда перевести заказ из `cooling` в `paid`, не проверяя изменение цены.
- [x] Шаг 13: Изменить TBankCredit `canceled`: освободить callback-платёж через `mark_released!`, отменить кредит и отменять заказ, пока кредит не достиг `committed`.
- [x] Шаг 14: Дополнить TBankCredit callback specs сценариями начала охлаждения, успешного завершения, ошибки capture, клиентской отмены и повторных callback; отдельно подтвердить, что изменение цены не блокирует `cooling -> paid`.
- [x] Шаг 15: Изменить Vanta `blank.cooled`: после проверки срока заморозить платеж, сохранить cooling metadata и перевести заказ из `approved` в `cooling` в одной транзакции.
- [x] Шаг 16: Изменить Vanta `blank.payed`: создать чек при завершении охлаждения, зафиксировать capture и всегда перевести заказ из `cooling` в `paid`, не проверяя изменение цены.
- [x] Шаг 17: Изменить Vanta `blank.canceled` во время `cooling`: заменить прямое обнуление заморозки на `mark_released!`, сбросить metadata callback-платежа и отменить заказ.
- [x] Шаг 18: Сохранить существующее поведение Vanta для `blank.rejected`, `blank.error` и `blank.auth_canceled` после capture, добавив регрессионные тесты на отсутствие неожиданных переходов заказа.
- [x] Шаг 19: Дополнить Vanta callback specs сценариями `approved -> cooling`, `cooling -> paid`, отмены с `mark_released!` и `order.cancel!`, повторных событий и изменения цены без блокировки перехода в `paid`.
- [x] Шаг 20: Унифицировать фискальные последствия по финальному бизнес-требованию: TBankCredit и Vanta на старте cooling только помечают платеж замороженным без `mark_authorized!` и фискализации; advanced receipt создается при подтвержденном завершении cooling.
- [x] Шаг 21: Проверить гонки callback: отмена и завершение охлаждения, повторное `signed`/`blank.payed`, завершение после уже отмененного заказа и отмена после capture; операции должны быть транзакционными и идемпотентными.
- [x] Шаг 22: Запустить focused specs для `Order`, `TBankCreditCallbackProcessor`, `VantaCallbackProcessor` и затронутых cooling/payment workflows через обязательный LevelTravel test workflow.
- [x] Шаг 23: Выполнить read-only финальную проверку diff на регрессии AASM, автобронирования, refunds, чеков, уведомлений, задач и state-based выборок.
- [x] Шаг 24: Обновить этот артефакт фактически выполненными шагами, результатами тестов, оставшимися рисками и точкой продолжения.
- [x] Шаг 25: Синхронизировать изменения `AGENTS.md`/`.agents/` в `../agents_md` через обязательный `leveltravel-agents-sync` workflow.
- [x] Шаг 26: Сделать старт cooling в TBankCredit и Vanta атомарным и идемпотентным относительно состояния заказа.
- [x] Шаг 27: Добавить строгую валидацию cooling-даты и исключить обход pending TBankCredit cooling в обычное подписание при невалидном `until`.
- [x] Шаг 28: Сериализовать конкурирующие start/pay/cancel callback единым порядком row locks и повторно проверять terminal states после блокировки.
- [x] Шаг 29: Ограничить отмену без refund-фискализации cooling-признаками конкретного callback-платежа для multi-payment заказов.
- [x] Шаг 30: Разделить переход заказа при cooling-отмене и фискализацию освобождения: обычную отмену фискализировать как раньше, новый cooling освобождать без чека, legacy cooling — с refund receipt при наличии старого purchase receipt.
- [x] Шаг 31: Удалить массовую разморозку из `Order#cancel`; кредитные callback освобождают только принадлежащий им cooling-платёж перед отменой заказа.
- [x] Шаг 32: Удалить отдельный recovery-сценарий captured cooling и внутренний event `stop_cooling`.
- [x] Шаг 33: Удалить TBank fallback по `waiting_cool_down`; при отмене обращаться напрямую к `payment.cooling_metadata?`, а старые frozen-платежи без metadata обрабатывать как обычные с refund-фискализацией.
- [x] Шаг 34: Заменить исключение при невалидном `commitCooldown.until` на `error_result` со статусом 400; удалить `validated_credit_cooldown_until` и читать дату внутри `start_cooling`.
- [x] Шаг 35: Полностью удалить `waiting_cool_down` из PaymentCredit и общей валидации capture; считать источником состояния только cooling metadata платежа.
- [x] Шаг 36: Запретить ручной capture cooling-платежа в manager UI и manager endpoint, а фоновое автосписание фильтровать по наличию cooling metadata.
- [x] Шаг 37: Изменить обычный TBankCredit `signed` без cooling: после `mark_authorized!` выполнить `capture!`, проверить `captured?` и только затем перевести заказ в `paid`.
- [x] Шаг 38: Отделить Vanta `blank.canceled` от `FAILURE_EVENTS`; освобождать frozen без чека, сбрасывать cooling metadata при наличии и отменять заказ. Считать `blank.auth_canceled` исключительно post-payment событием: refund только captured-платежа, для non-captured ничего не менять.
- [x] Шаг 39: Определять повторный `blank.cooled` по cooling metadata конкретного платежа и возвращать success без запроса `payment_info`, сохранив повторную проверку после row locks для защиты от гонки.
- [x] Шаг 40: Разрешить отдельному Vanta-платежу сохранить заморозку и cooling metadata, когда заказ уже `cooling` из-за другого платежа; переход заказа выполнять через идемпотентный `transition_order(:start_cooling)`.
- [x] Шаг 41: В TBankCredit `canceled` отменять заказ для любого состояния, кроме `committed`; `signed` без успешного capture также должен отменять заказ.

## 4. Заметки для восстановления сессии

### Текущее состояние проекта и уже выполненная работа

- Основная реализация опубликована в PR #25744; локально подготовлены дополнительные правки по review атомарности старта cooling и валидации даты, пока не закоммиченные и не отправленные.
- Добавлен AASM-state `cooling`, внутренний event `start_cooling`, классификация `Order.not_paid`, переходы в `paid` и `canceled`, state logging, переводы, клиентские стили и progress step.
- `cooling -> paid` скрыт из ручных действий и защищен guard, который принимает конкретный payment из callback и проверяет принадлежность заказу, `captured?` и cooling metadata.
- `order.cancel!` не освобождает cooling-платежи массово; обработкой финансового состояния владеет callback конкретного платежа.
- TBankCredit:
  - начинает cooling только при валидном pending `commitCooldown`;
  - проверяет `commitCooldown.until` до локального `credit.sign!`, возвращает `400 invalid_commit_cooldown` и не превращает невалидный cooling callback в обычное подписание;
  - блокирует заказ при старте cooling, допускает только `approved -> cooling` и идемпотентный retry уже в `cooling`;
  - сериализует start/cancel/complete через row locks `payment -> credit -> order` и после блокировки перечитывает актуальные состояния;
  - определяет cooling-фискализацию напрямую по metadata callback-платежа, не по статусу заказа или legacy-флагу кредита;
  - при освобождении обычного платежа сохраняет refund-фискализацию; для cooling-платежа создает refund receipt только если у платежа уже существует advanced purchase receipt от legacy-сценария;
  - при `canceled` освобождает и сбрасывает callback-платёж, отменяет кредит и вызывает отмену заказа, если кредит не `committed`; только уже успешно списанный кредит сохраняет статус заказа;
  - на старте cooling напрямую сохраняет заморозку без `mark_authorized!` и advanced receipt;
  - создает advanced receipt только после успешного завершения cooling/capture;
  - без `commitCooldown` вызывает `mark_authorized!`, затем `capture!`; успешное списание переводит кредит в `committed`, платеж в `captured`, а заказ в `paid`;
  - после `capture!` проверяет `payment.reload.captured?`;
  - сохраняет metadata при ошибке capture для безопасного retry;
  - больше не записывает и не читает `waiting_cool_down`; старый frozen-платёж без metadata освобождает как обычный платеж с refund-фискализацией.
- Общая capture-валидация платежа больше не знает про охлаждение:
  - менеджерка получает boolean `cooling`, не показывает действие capture и отклоняет прямой запрос на сервере;
  - `PaymentCapturingService` исключает любой платеж с cooling metadata, даже после истечения указанного срока;
  - финальный банковский callback вызывает обычный `capture!` без специального bypass-параметра.
- Vanta:
  - напрямую сохраняет заморозку без `mark_authorized!`/advanced receipt и переводит заказ в `cooling` на `blank.cooled`;
  - блокирует заказ при старте cooling, допускает только `approved -> cooling` и идемпотентный retry уже в `cooling`;
  - сериализует cooled/payed/failure/auth-canceled через row locks `payment -> order` и не выполняет capture после конкурентной отмены;
  - определяет текущий cooling по metadata конкретного Vanta-платежа, а legacy frozen-платёж освобождает по его собственному callback независимо от текущего состояния заказа;
  - переводит в `paid` только после подтвержденного capture;
  - явно вызывает `mark_released!`, освобождает и сбрасывает только callback-платёж, отменяя заказ на `blank.canceled`;
  - обрабатывает `blank.canceled` отдельным методом: независимо от cooling-признака освобождает frozen без refund receipt, сбрасывает metadata только при наличии и вызывает стандартную отмену заказа;
  - обрабатывает `blank.auth_canceled` только для captured-платежа: выполняет автоматический refund либо логирует необходимость ручного; non-captured платеж и заказ не изменяет;
  - оставляет в `FAILURE_EVENTS` только `blank.rejected` и `blank.error`, которые не меняют cooling-состояние;
  - создает advanced receipt при завершении cooling на `blank.payed`, как TBankCredit;
  - не создает чек при неподтвержденном capture и восстанавливает его при retry после уже сохраненного capture;
  - блокирует и перечитывает payment перед capture, а `PaymentExport` создает только после подтвержденного capture, исключая дубли на параллельных callback;
  - не фискализирует повторный `blank.cooled`, включая callback после истечения cooling date;
  - игнорирует поздний `blank.payed` после отмены;
  - сохраняет прежнее поведение `blank.rejected`, `blank.error` и captured `blank.auth_canceled`.
- Мобильное представление показывает frozen сумму как «Заморожено», скрывает нулевую задолженность и повторные способы оплаты в `cooling`.
- Изменение цены package не проверяется и не блокирует успешное завершение cooling.
- Миграция БД и изменение PAPI v3 schema не потребовались: `orders.state` строковый, а документированное поле state не ограничено enum.

### Проверки

- PASS focused check после перехода на payment-specific отмену без отмены заказа, 74 examples, 0 failures:
  - `source ./lt.sh && bundle exec rspec spec/apis/t_bank_credit_callback_processor_spec.rb spec/apis/vanta_callback_processor_spec.rb spec/models/order_spec.rb:415 spec/workers/yandex_split_cooling_checker_worker_spec.rb spec/controllers/vanta_notifications_controller_spec.rb spec/controllers/t_bank_credit_notifications_controller_spec.rb spec/serializers/order_serializer_spec.rb`
- PASS: Ruby syntax для измененных Ruby-файлов.
- PASS: `config/locales/ru.yml` загружается с разрешенными существующими YAML aliases.
- PASS: `git diff --check`.
- PASS focused check: `bundle exec rspec spec/apis/t_bank_credit_callback_processor_spec.rb` — 29 examples, 0 failures.
- PASS focused check: `bundle exec rspec spec/controllers/t_bank_credit_notifications_controller_spec.rb` — 5 examples, 0 failures.
- PASS focused check: `bundle exec rspec spec/controllers/t_bank_credit_notifications_controller_spec.rb spec/services/payment_capturing_service_spec.rb` — 9 examples, 0 failures.
- PASS focused check: `bundle exec rspec spec/apis/vanta_callback_processor_spec.rb spec/controllers/vanta_notifications_controller_spec.rb` — 20 examples, 0 failures.
- PASS focused check: `bundle exec rspec spec/apis/t_bank_credit_callback_processor_spec.rb spec/controllers/t_bank_credit_notifications_controller_spec.rb` — 36 examples, 0 failures.
- PASS: финальный read-only working-tree `leveltravel-pr-review` против `origin/develop`; блокирующих замечаний нет.
- Полный локальный gate не запускался; authoritative TeamCity `rails-rspec` остаётся pending до публикации PR.
- Отдельно воспроизводятся два существующих несвязанных падения `Order#booking!` в `spec/models/order_spec.rb:27` и `:40`: ожидаемый `JobEnqueuer` не вызывается; измененные строки `booking!` не затрагивают.

### Оставшиеся риски и точка продолжения

- Перед merge обязателен authoritative remote TeamCity `rails-rspec`.
- При подготовке PR повторить обычный `leveltravel-pr-workflow`; реализация, focused tests и read-only review уже готовы.
- Не расширять state на Paylate, Yandex Split или другие процессоры в рамках LT-52093.

### Критически важные принятые решения

- Новый state называется `cooling`, отображается как «Охлаждение» и относится к неоплаченным.
- Вход в `cooling` происходит только при подтвержденном наличии активного периода охлаждения, а не при простом одобрении рассрочки.
- TBankCredit без охлаждения после подписания сразу авторизует и списывает платеж; заказ становится `paid` только после подтвержденного capture.
- Единственный источник состояния охлаждения — metadata платежа; `waiting_cool_down` удален.
- TBankCredit `canceled` освобождает только callback-платёж и сбрасывает его metadata; заказ отменяется во всех состояниях кредита, кроме `committed`. Vanta `blank.canceled` после очистки callback-платежа отменяет заказ.
- Переход в `paid` после cooling выполняется только после успешной финансовой фиксации платежа.
- Рост или снижение цены package не создает отдельного сценария: после успешной финансовой фиксации заказ всегда переводится в `paid`.
- Cooling workflow не сохраняет snapshot цены и не обращается к `OrderPriceChange`.
- После `cooling -> paid` используется существующая автобронь и её текущие правила актуализации и проверки цены; менять их в этой задаче не нужно.

### Точка продолжения при восстановлении сессии

1. Проверить `git status` и актуальность рабочего дерева.
2. При необходимости подготовить commit/push/PR через `leveltravel-pr-workflow`.
3. Дождаться authoritative TeamCity `rails-rspec` и разобрать только относящиеся к diff падения.
4. Не расширять задачу на Paylate, Yandex Split или другие платежные методы.
5. Не добавлять проверки изменения цены: успешное завершение cooling всегда должно завершать платеж и переводить заказ в `paid`.
