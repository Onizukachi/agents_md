1. **Бизнес контекст для задачи**
   Нужно явно зафиксировать, что пользовательские ссылки на "артефакты" относятся к файлам планов и контекста задач в `.agents/tasks`.

2. **Ключевые решения и обоснования**
   - "Артефакт N" трактуется как `.agents/tasks/task-N.md`, чтобы команды вида "восстанови контекст из артефакта 1" и "удали артефакт 2" были однозначными.
   - Правило нужно продублировать и в `AGENTS.md`, и в `.agents/tasks/PLANS.md`, чтобы оно было видно и в общих инструкциях, и рядом с процессом создания самих артефактов.

3. **Структурированный список задач**
   - [x] Шаг 1: Проверить, где в репозитории уже описаны task-артефакты.
   - [x] Шаг 2: Добавить явное правило про соответствие "артефакт N" -> `.agents/tasks/task-N.md`.
   - [x] Шаг 3: Синхронизировать `AGENTS.md` и `.agents/` в `../agents_md`.

4. **Заметки для восстановления сессии**
   - В репозитории файлы задач лежат в `.agents/tasks/`.
   - На момент старта в папке был только `PLANS.md`; для текущей работы создан `task-1.md`.
   - Правила про артефакты добавлены в `AGENTS.md` и `.agents/tasks/PLANS.md`.
   - После изменения файлов зеркало `../agents_md` синхронизировано по навыку `leveltravel-agents-sync`.
