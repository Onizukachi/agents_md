# LT-52595: Поддержать загрузку реестров операций HBPro

## 1. Бизнес-контекст

Бухгалтерии Level Travel нужно загружать реестры операций нового поставщика HBPro через существующую ActiveAdmin-страницу «Загрузка реестров операций». Реестр должен проходить тот же конвейер, что и недавно добавленный Ostrovok: предварительная фильтрация и сохранение файла, извлечение `OperatorOperation`, обогащение через Dynamics, сопоставление с заказом и создание либо возврат `OperatorPayment`.

Исходный пример: `/Users/hikaru/Downloads/HBPro от 20.07.2026.xlsx`. В книге один лист `Лист1`, первая строка содержит заголовки `№`, `Объект расчетов`, `Заказ клиента`, `Сумма`. В приложенном файле две положительные операции; примера отрицательной суммы нет.

Формат значимых полей:

- `Объект расчетов`: текст вида `Заказ клиента № ХП00260717-00679  от 17 июля 2026 г.`; из него нужно извлечь календарную дату операции;
- `Заказ клиента`: внешний номер заказа поставщика, в примере Excel хранит его как число (`2256085`);
- `Сумма`: строка вида `1 288,60(RUB)` с неразрывным пробелом, десятичной запятой и суффиксом валюты;
- положительная сумма означает оплату и записывается одновременно в `captured_amount` и `gross_captured_amount`;
- отрицательная сумма означает возврат, её абсолютное значение записывается одновременно в `refunded_amount` и `gross_refunded_amount`;
- валюта всегда `RUB`, курс — `1`.

Параметры интеграции:

- имя модуля в Ruby: `Accounting::Hbpro`, отображаемое имя поставщика: `HBPro`;
- `OPERATOR_ID = 78`;
- `SUPPLIER_ID = 104` для самих `OperatorOperation`;
- `DYNAMICS_ORGANIZATION_ID = 19` для получения booking ID через Dynamics;
- `ORDER_SUPPLIER_ID = Accounting::Constants::Gortravel::SUPPLIER_ID` (`89`) для поиска заказа;
- `OPERATOR_PAYMENT_SUPPLIER_ID = Accounting::Constants::Gortravel::SUPPLIER_ID` (`89`) для создаваемых операторских платежей;
- `RELEASE_DATE = Date.new(2026, 7, 1)`;
- платежи HBPro должны иметь те же source flags, что Ostrovok: `deposit: true`, `payment_order: false`, `virtual: false`.

## 2. Ключевые решения и обоснования

1. **Встроить HBPro в существующий `Accounting::OperatorOperationsProcessor`.** Новый реестр использует последовательность `%i(extract enrich manage)`, чтобы не создавать отдельную оркестрацию и сохранить единое поведение загрузчика, логирования и обработки ошибок.

2. **Использовать `Hbpro` в именах Ruby-констант и `HBPro` в пользовательских текстах.** В проекте уже существует namespace `OperatorAPI::Hbpro`; такое написание избегает второго варианта inflection и при этом сохраняет корректное название поставщика в интерфейсе и логах.

3. **Разделить организацию операции и организации заказа/платежа.** `OperatorOperation` хранит новый `supplier_id = 104`, но заказ ищется и `OperatorPayment` создаётся с организацией Gortravel (`89`). Это повторяет подтверждённую бизнес-схему Ostrovok и требует явных `ORDER_SUPPLIER_ID` и `OPERATOR_PAYMENT_SUPPLIER_ID`.

4. **Матчить заказ через Dynamics, а не напрямую по `Заказ клиента`.** Значение `Заказ клиента` сохраняется как `supplier_order_id`, передаётся в Dynamics с `organization_id = 19`, из ответа берётся `booking.booking_id`, после чего заказ ищется по `operator_booking_id`, оператору `78` и supplier ID `89`. При нескольких совпадениях сохраняется текущая логика `DynamicsEnricher`: приоритет заказу с `booked_at`, затем самому раннему по `created_at`, затем существующий production fallback через Solr.

5. **Парсить дату явно из русского текста.** Нельзя полагаться на системную locale в `Date.parse`. Extractor должен выделить день, русское название месяца и год, преобразовать месяц через локальную таблицу и создать дату. Некорректное или отсутствующее значение должно приводить к понятной ошибке строки/операции, а не к молчаливой подстановке текущей даты.

6. **Нормализовать денежную строку до `BigDecimal`.** Перед преобразованием нужно удалить обычные и неразрывные пробелы, суффикс `(RUB)`, заменить десятичную запятую точкой и сохранить знак. После определения типа операции в суммы записывается абсолютное значение. Нулевая либо нераспознанная сумма не должна создавать оплату или возврат.

7. **Заполнять одновременно обычную и gross-сумму.** Для оплаты `captured_amount == gross_captured_amount`, для возврата `refunded_amount == gross_refunded_amount`. Благодаря этому стандартная логика capture/refund сможет работать с HBPro без нового supplier-specific переключения сумм, а данные операции останутся полными.

8. **Стабильно дедуплицировать строки независимо от имени загруженного файла.** `external_id` нужно вычислять через SHA-256 из имени поставщика и нормализованных значений даты, supplier order ID и подписанной суммы. Повторная загрузка того же реестра или той же строки под другим именем не должна создавать дубли. Знак суммы должен входить в hash, чтобы оплата и возврат одной суммы не совпали.

9. **Сохранить идентификаторы Excel строками на этапе `FileProcessor`.** Уже существующая принудительная запись строк защищает `Заказ клиента` от scientific notation и потери точности. Extractor дополнительно должен корректно нормализовать как строковое, так и числовое значение supplier order ID.

10. **Сделать выбор supplier ID для заказа и платежа расширяемым.** Текущий код содержит специальную ветку только для Ostrovok. При добавлении HBPro нужно либо обобщить выбор через supplier constants, либо аккуратно добавить HBPro во все соответствующие ветки; тестами обязательно зафиксировать отсутствие изменений для остальных поставщиков.

11. **Не добавлять gem и не менять схему БД.** Формат можно разобрать имеющимися Roo, ActiveSupport и стандартными классами даты/чисел; новые persisted-поля не требуются.

## 3. Структурированный список задач

- [ ] Шаг 1: Добавить в `Accounting::Constants` отображаемое имя `HBPro`, пару `operator_78_supplier_104`, включить поставщика в `operations_loader` и `operations_loader_filter`.
- [ ] Шаг 2: Добавить `Accounting::Constants::Hbpro` с `OPERATOR_ID = 78`, `SUPPLIER_ID = 104`, `ORDER_SUPPLIER_ID = Gortravel::SUPPLIER_ID`, `OPERATOR_PAYMENT_SUPPLIER_ID = Gortravel::SUPPLIER_ID`, `DYNAMICS_ORGANIZATION_ID = 19`, `RELEASE_DATE = Date.new(2026, 7, 1)`, `PRECISION = 2`, `%i(extract enrich manage)`, S3/Redis-настройками и точными regex заголовков реестра.
- [ ] Шаг 3: Добавить `Accounting::Hbpro::FileUploader` на базе `Accounting::BaseFileUploader` и констант HBPro.
- [ ] Шаг 4: Добавить fixture или тестовый XLSX с реальным форматом HBPro, не привязывая specs к файлу из `/Users/hikaru/Downloads`; включить положительную, отрицательную, нулевую и некорректную суммы, разные месяцы и числовой supplier order ID.
- [ ] Шаг 5: Реализовать и unit-тестами зафиксировать парсер `Объект расчетов`: извлечение дня, русского месяца и года, получение даты и явную ошибку при неподдерживаемом формате.
- [ ] Шаг 6: Реализовать и unit-тестами зафиксировать парсер `Сумма`: обычные/неразрывные пробелы, десятичная запятая, `(RUB)`, отрицательный знак, целые значения и отказ от создания операции для нуля/невалидного значения.
- [ ] Шаг 7: Реализовать `Accounting::Hbpro::Extractor`: Roo header matching, нормализацию supplier order ID, определение capture/refund по знаку суммы, заполнение обычной и gross-суммы абсолютным значением, `RUB`, курс `1`, operator/supplier IDs, `report_id` и `cleared: false`.
- [ ] Шаг 8: Добавить стабильный SHA-256 `external_id` из нормализованных бизнес-полей и specs на идемпотентную повторную загрузку, одинаковые данные под разными именами файлов и различие capture/refund.
- [ ] Шаг 9: Добавить обработку отсутствующих обязательных заголовков с понятным сообщением, содержащим ожидаемые названия колонок, и покрыть её extractor spec.
- [ ] Шаг 10: Добавить `Accounting::Hbpro::Enricher` на базе `Accounting::DynamicsEnricher`, передающий `organization_id = 19` и переопределяющий supplier для поиска заказа на `ORDER_SUPPLIER_ID = 89`.
- [ ] Шаг 11: Добавить enricher spec: Dynamics получает `supplier_order_id` и organization `19`, возвращённый booking ID матчится с заказом оператора `78` и supplier ID `89`, операция получает `operator_booking_id`, `order_id`, `RUB` и курс `1`.
- [ ] Шаг 12: Расширить выбор `operator_payment_supplier_id`, чтобы HBPro-операция создавала и находила платежи организации `89`; сохранить текущее поведение Ostrovok и остальных поставщиков.
- [ ] Шаг 13: Добавить `SUPPLIER_ID = 104` в депозитную ветку `OperatorPayment.payment_source_flags_for`, чтобы платеж HBPro создавался с `deposit: true`, `payment_order: false`, `virtual: false`.
- [ ] Шаг 14: Добавить factory traits для HBPro `OperatorOperation` и `OperatorOrganization`, используя доменное имя и подтверждённые ID без жёсткой зависимости от локального XLSX.
- [ ] Шаг 15: Добавить model specs управления HBPro capture: платёж создаётся на supplier ID `89`, получает обе суммы из реестра, правильные source flags, дату, валюту и курс, а операция связывается с платежом и очищается.
- [ ] Шаг 16: Добавить model specs управления и отмены HBPro refund: отрицательная сумма превращается в положительную сумму возврата, находится HBPro-платёж организации `89`, возврат проводится и затем корректно отменяется.
- [ ] Шаг 17: Добавить regression specs, подтверждающие, что сопоставление supplier ID и выбор сумм для Ostrovok, Gortravel и остальных существующих загрузчиков не изменились после обобщения логики.
- [ ] Шаг 18: Проверить вручную через объект `Accounting::OperationsLoader::FileProcessor`, что приложенный файл распознаётся как HBPro, сохраняет обе строки, показывает корректный preview и формирует XLSX без преобразования номера заказа и суммы Excel.
- [ ] Шаг 19: Через `.agents/skills/leveltravel-tests` запустить focused specs extractor, enricher, `OperatorOperation` и `OperatorPayment`; затем выполнить предписанный CI-equivalent RSpec gate.
- [ ] Шаг 20: Просмотреть итоговый diff: отсутствие новых зависимостей и миграций, корректность ActiveAdmin-списка поставщиков, S3/Redis ключей, логирования, дедупликации, обработки ошибочных строк и отсутствие изменений несвязанных файлов.

## 4. Заметки для восстановления сессии

### Текущее состояние проекта и выполненная работа

- Рабочая ветка на момент составления плана: `hotfix/LT-52595-operator-operations-loader-support-hbpro_dev`, основана на актуальном `develop` (`774063e4d7`).
- В рабочем дереве до создания этого артефакта не было изменений.
- Продуктовый код и specs для LT-52595 ещё не изменялись; выполнен только анализ существующей реализации Ostrovok и приложенного XLSX.
- В репозитории уже присутствует реализация Ostrovok из коммита `b8853ac65518c3ba7ca872e91fff63f1ea120681`: её extractor, enricher, file uploader и model specs являются основным образцом.
- Приложенный файл успешно открыт через Roo без запуска Rails. Он содержит один лист, заголовок и две положительные строки; отрицательные, нулевые и повреждённые значения необходимо синтезировать в тестах.
- `bundle exec` сейчас не стартует на host из-за отсутствующего checkout git-gem `markerb`; при реализации тесты нужно запускать по проектному Docker-процессу через `source ./lt.sh` и skill `leveltravel-tests`, а не делать вывод о состоянии suite из host Bundler.

### Критически важные принятые решения

- Namespace — `Accounting::Hbpro`, пользовательское название — `HBPro`.
- `OperatorOperation`: operator `78`, supplier `104`.
- Dynamics: organization `19`.
- Поиск заказа и `OperatorPayment`: supplier `89` (`Gortravel::SUPPLIER_ID`).
- Дата начала обработки/поиска возвратов: `01.07.2026`.
- Платежи депозитные, как у Ostrovok.
- Дата берётся только из текста `Объект расчетов`; системная locale не должна влиять на парсинг русских месяцев.
- Знак `Сумма` определяет тип операции. В persisted amount columns сохраняются абсолютные значения; для capture и refund заполняются одновременно обычная и gross-колонки.
- Валюта всегда `RUB`, курс всегда `1`.
- `external_id` должен зависеть от нормализованных данных строки, а не от имени файла или номера строки.
- Новые gems и migration не нужны.

### Точка продолжения после восстановления сессии

Начать с шагов 1-4: добавить константы/регистрацию поставщика и локальный тестовый fixture. Затем реализовать parsers и extractor вместе со specs, после чего перейти к Dynamics enrichment и model-level управлению платежами. Перед любыми LT/Rails-командами выполнить `source ./lt.sh`; тесты запускать строго по `.agents/skills/leveltravel-tests`. После любых дальнейших изменений этого task-файла или других файлов в `.agents/` повторить `.agents/skills/leveltravel-agents-sync`.
