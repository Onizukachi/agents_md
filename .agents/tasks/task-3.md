# История ставок НДС для operator_organizations

## 1. Бизнес-контекст задачи

Для юридического лица туроператора из таблицы `operator_organizations` нужно хранить не одно текущее значение НДС, а историю ставок с периодами действия.

Ставка должна выбираться по дате создания заказа `receipt.order.created_at`. Менеджер должен иметь возможность редактировать историю ставок прямо в текущей форме `operator_organization` в ActiveAdmin без перехода в отдельный ресурс.

Если у ставки не указана дата окончания, она действует бессрочно до момента, пока не будет заведена следующая ставка. При создании новой ставки предыдущую открытую ставку нужно автоматически закрывать датой `starts_at - 1 день`.

Изменение должно работать для `Uniteller`. Для `CheckOnline` в рамках этой задачи код не меняется, но в плане и заметках нужно сохранить технический долг: для него требуется отдельный маппинг пользовательской ставки VAT в `TaxId`.

Текущее поле `operator_organizations.vat` пока остается в базе для безопасного перехода, но должно быть убрано из формы и отображения в админке. Перенос существующих значений в новую таблицу будет выполняться отдельной rake-задачей: для каждой организации с заполненным `vat` создается бессрочная запись со `starts_at = 2026-01-01`.

## 2. Ключевые решения и обоснования

1. Историю ставок хранить в отдельной таблице `operator_organization_vat_rates`, а не расширять `operator_organizations`.
   Обоснование: требуется несколько периодов для одной организации, валидация непересекающихся интервалов и безопасная миграция без потери истории.

2. Дату действия ставки определять по `receipt.order.created_at.to_date`.
   Обоснование: это явно согласовано для бизнес-логики, и сравнение по дню убирает неоднозначность на границе суток.

3. Настройку ставок сделать через nested form в `app/admin/operator_organizations.rb`.
   Обоснование: пользователь просил оставить управление в текущей форме, а в проекте уже используются `f.has_many` и `accepts_nested_attributes_for`.

4. Пересечения периодов для одной организации запрещать полностью.
   Обоснование: это исключает неявный выбор ставки и упрощает поведение фискализации.

5. При сохранении новой ставки с `starts_at = X` автоматически закрывать предыдущую открытую ставку датой `X - 1 день`.
   Обоснование: это соответствует согласованному поведению "действует бесконечно до следующей ставки" и снижает ручную нагрузку на менеджера.

6. Допустимые значения ставки ограничить целыми числами от 1 до 100.
   Обоснование: это явно подтверждено пользователем; вариант "без НДС" в этой задаче не поддерживается.

7. Старое поле `operator_organizations.vat` не удалять в рамках этой задачи.
   Обоснование: оно нужно как временный fallback для миграционного периода и rake-переноса, но не должно больше редактироваться через админку.

8. Rake-задачу миграции сделать отдельно от схемной миграции.
   Обоснование: пользователь явно задал способ переноса данных и дату начала исторических записей `2026-01-01`.

9. `CheckOnline` пока оставить без изменений и зафиксировать это как технический долг.
   Обоснование: пользователь отдельно подтвердил, что в рамках этой задачи достаточно не менять текущую интеграцию `CheckOnline`, а только оставить заметку на будущее.

10. При вставке ставки внутрь уже существующей истории менеджер должен сам явно указать `ends_at`; автоматически закрывается только предыдущая открытая ставка.
   Обоснование: это подтверждено пользователем и не позволяет неявно переписывать уже завершенные интервалы.

11. В ActiveAdmin ставки нужно отображать и редактировать в порядке `starts_at DESC`.
   Обоснование: пользователь подтвердил, что сначала должны идти самые новые периоды.

## 3. Структурированный список задач

- [ ] Шаг 1: Создать миграцию для таблицы `operator_organization_vat_rates` с полями `operator_organization_id`, `vat`, `starts_at`, `ends_at` и индексами для выборки по организации и периоду.
- [ ] Шаг 2: Добавить модель `OperatorOrganizationVatRate` с ассоциациями, базовыми валидациями дат и валидацией диапазона `vat` от 1 до 100.
- [ ] Шаг 3: Реализовать валидацию непересекающихся периодов для ставок одной организации, включая открытые интервалы.
- [ ] Шаг 4: Добавить в `OperatorOrganization` ассоциацию `has_many :vat_rates`, nested attributes и метод выбора ставки на дату `vat_on(date)`.
- [ ] Шаг 5: Реализовать автозакрытие предыдущей открытой ставки при создании новой ставки с более поздней датой начала.
- [ ] Шаг 6: Обновить `app/admin/operator_organizations.rb`: убрать старое поле `vat` из `permit_params`, `index`, `show`, `form`; добавить nested form и отображение истории ставок в порядке `starts_at DESC`.
- [ ] Шаг 7: Обновить места фискализации в `Uniteller`, чтобы VAT поставщика выбирался через историю ставок по `receipt.order.created_at.to_date`.
- [ ] Шаг 8: Оставить в коде заметку о том, что `CheckOnline` требует отдельного маппинга VAT в `TaxId`, но не менять текущую интеграцию в рамках этой задачи.
- [ ] Шаг 9: Добавить rake-задачу переноса данных из `operator_organizations.vat` в новую таблицу со `starts_at = 2026-01-01`.
- [ ] Шаг 10: Добавить/обновить фабрики и RSpec-тесты для модели ставок, выбора ставки по дате, автозакрытия периодов и `Uniteller`.
- [ ] Шаг 11: Запустить точечные проверки: миграция, релевантные спеки, `git diff --check`.
- [ ] Шаг 12: После изменения `.agents/**/*` синхронизировать `AGENTS.md` и `.agents/` в `../agents_md/`, выполнить там `git pull`, затем commit и push зеркала согласно правилам проекта.

## 4. Заметки для восстановления сессии

Рабочая директория: `/home/hikaru/projects/work/leveltravel`.

На момент создания файла код еще не менялся, кроме подготовки плана в этой задаче. Уже исследованы текущие точки интеграции:

- `app/models/operator_organization.rb`
  - Сейчас хранит плоский `vat` как `integer`.
  - Истории ставок нет.
- `app/admin/operator_organizations.rb`
  - Сейчас показывает и редактирует поле `vat` напрямую.
  - В проекте есть рабочие примеры nested form через `f.has_many`.
- `app/apis/fiscal_processor/uniteller.rb`
  - Сейчас берет VAT напрямую через `line_item.supplier&.vat`.
- `app/apis/fiscal_processor/check_online.rb`
  - Сейчас для этих строк вообще не использует ставку поставщика и отправляет фиксированный `TaxId`.
- `app/apis/fiscal_processor/constants.rb`
  - Для `CheckOnline` виден только `VATS[:without] = 4`, поэтому маппинг пользовательских ставок пока не определен.
- `db/schema.rb`
  - В `operator_organizations` уже есть столбец `vat`, который пока остается в базе.

Уже согласованные решения:

- история ставок хранится в отдельной таблице;
- дата выбора ставки: `receipt.order.created_at.to_date`;
- управление ставками остается в текущей форме `operator_organization`;
- пересечения периодов запрещены;
- предыдущая открытая ставка закрывается автоматически;
- перенос старых данных делается rake-задачей;
- для переноса используются только значения `vat` от 1 до 100;
- значение "без НДС" не поддерживается;
- старое поле `operator_organizations.vat` пока не удаляется, только уходит из админки;
- изменение нужно сделать для `Uniteller`;
- `CheckOnline` пока остается без изменений, но в коде и плане нужно сохранить заметку о будущем маппинге VAT в `TaxId`;
- при вставке ставки в середину истории менеджер сам указывает `ends_at`, автоматически закрывается только предыдущая открытая ставка;
- ставки в админке сортируются по `starts_at DESC`.

Если сессия прервется, продолжать с:

1. синхронизации этого файла в `../agents_md/` по обязательному правилу проекта;
2. перехода к шагу 1 из списка задач и начала реализации схемы данных;
3. отдельно не забыть оставить в коде заметку про будущий `CheckOnline` VAT mapping.
